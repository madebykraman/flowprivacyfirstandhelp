name: Deploy Ritmi

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Prepare launch bundle
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path
          p=Path('app-complete.js')
          s=p.read_text()
          s=s.replace("return'<option '+(l.discharge===x?'selected':'')+'>'+x+'</option>').join('')", "return'<option '+(l.discharge===x?'selected':'')+'>'+x+'</option>'}).join('')")
          s=s.replace("$('[data-overlay]')?.remove();", "var ov=$('[data-overlay]');if(ov)ov.remove();")
          p.write_text(s)
          PY
      - name: Verify product assets
        shell: bash
        run: |
          test -s index.html
          test -s ritmi-v3.css
          test -s ritmi-3-3.css
          test -s ritmi-next.css
          test -s ritmi-3-5.css
          test -s app-complete.js
          grep -q 'ritmi-3-5.css?v=65' index.html
          grep -q 'ritmi-enhancements.js?v=65' index.html
          grep -q "const CACHE='ritmi-static-v3.5.0'" sw.js
          grep -q 'quiet-nav' ritmi-enhancements.js
          grep -q 'Primary navigation' ritmi-enhancements.js
          grep -q 'grid-template-columns:repeat(4,1fr)' ritmi-3-5.css
      - name: Run launch checks
        shell: bash
        run: node tests/launch-hardening.test.js
      - name: Setup Pages
        uses: actions/configure-pages@v5
        with:
          enablement: true
      - name: Upload static site
        uses: actions/upload-pages-artifact@v4
        with:
          path: .
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
